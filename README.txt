splitpdf is a simple bash script which uses ghostscript to split a PDF
file into its pages, one file per page.  The programs needed to use
splitpdf besides bash are

* gs
* awk

splitpdf is in the public domain, use it for whatever.
